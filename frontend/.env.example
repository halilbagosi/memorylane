# Copy this file to .env and fill in your machine's local IP address.
# Windows: run `ipconfig`  → find IPv4 Address
# Mac:     run `ifconfig`  → find inet under en0

EXPO_PUBLIC_API_BASE_URL=http://YOUR_LOCAL_IP:3000
